# 台股資料管理系統 (Stock Management System)

一個現代化的台股資料管理網頁應用程式，提供股票資料查詢、三大法人買賣超、融資融券等功能。

## 📋 專案簡介

本專案包含前端和後端兩個部分：
- **前端**: React + TypeScript + Vite
- **後端**: Node.js + Express + TypeScript

## ✨ 主要功能

- 📊 **成交量資料** - 每日股票成交量追蹤
- 🏦 **三大法人買賣** - 外資、投信、自營商買賣超資料
- 💰 **融資資料** - 融資餘額與變化
- 📉 **融券資料** - 融券餘額與變化
- 🔍 **股票搜尋** - 快速搜尋與篩選功能
- 📈 **歷史資料查詢** - 查看歷史趨勢與統計
- 🌓 **深色模式** - 支援淺色/深色主題切換

## 🛠️ 技術棧

### 前端
- React 18
- TypeScript
- Vite
- React Router
- Axios
- CSS Modules

### 後端
- Node.js
- Express
- TypeScript
- CORS
- dotenv

## 📦 專案結構

```
StockProject/
├── frontend/          # 前端專案
│   ├── src/
│   │   ├── components/   # React 元件
│   │   ├── pages/        # 頁面元件
│   │   ├── services/     # API 服務
│   │   ├── styles/       # 樣式檔案
│   │   ├── types/        # TypeScript 型別
│   │   └── utils/        # 工具函數
│   └── package.json
│
├── backend/           # 後端專案
│   ├── src/
│   │   ├── controllers/  # 控制器
│   │   ├── routes/       # 路由
│   │   ├── services/     # 業務邏輯
│   │   └── app.ts        # 應用程式進入點
│   └── package.json
│
└── README.md
```

## 🚀 快速開始

### 前置需求

- Node.js 18+ 
- npm 或 yarn

### 安裝步驟

1. **Clone 專案**
```bash
git clone https://github.com/s0929053823/stockProject.git
cd stockProject
```

2. **安裝前端依賴**
```bash
cd frontend
npm install
```

3. **安裝後端依賴**
```bash
cd ../backend
npm install
```

4. **設定環境變數**

前端 (frontend/.env):
```env
VITE_API_BASE_URL=http://localhost:3000/api/v1
```

後端 (backend/.env):
```env
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173
```

5. **啟動開發伺服器**

啟動後端 (在 backend 資料夾):
```bash
npm run dev
```

啟動前端 (在 frontend 資料夾):
```bash
npm run dev
```

6. **開啟瀏覽器**

前端: http://localhost:5173
後端 API: http://localhost:3000

## 📝 API 文件

### 股票管理

- `GET /api/v1/stocks` - 取得股票清單
- `GET /api/v1/stocks/:stockCode` - 取得特定股票資訊
- `POST /api/v1/stocks` - 新增股票
- `PUT /api/v1/stocks/:stockCode` - 更新股票資訊
- `DELETE /api/v1/stocks/:stockCode` - 刪除股票

### 每日交易資料

- `GET /api/v1/stocks/:stockCode/trading` - 取得交易資料
- `POST /api/v1/stocks/:stockCode/trading` - 新增交易資料

### 三大法人資料

- `GET /api/v1/stocks/:stockCode/institutional` - 取得法人資料
- `POST /api/v1/stocks/:stockCode/institutional` - 新增法人資料

### 融資融券資料

- `GET /api/v1/stocks/:stockCode/margin` - 取得融資融券資料
- `POST /api/v1/stocks/:stockCode/margin` - 新增融資融券資料

### 綜合查詢

- `GET /api/v1/stocks/:stockCode/complete` - 取得股票完整資料

## 🔧 開發指令

### 前端

```bash
npm run dev      # 啟動開發伺服器
npm run build    # 建置生產版本
npm run preview  # 預覽生產版本
```

### 後端

```bash
npm run dev      # 啟動開發伺服器 (with nodemon)
npm run build    # 編譯 TypeScript
npm start        # 啟動生產伺服器
```

## 🎨 設計系統

專案使用完整的設計系統，包含：
- 色彩系統 (主色、輔助色、中性色)
- 字體系統 (Inter, Noto Sans TC)
- 間距系統 (4px 基準)
- 陰影系統
- 圓角系統
- 響應式斷點

## 📱 響應式設計

支援以下裝置尺寸：
- 手機: < 768px
- 平板: 768px - 1024px
- 桌面: > 1024px

## 🌐 瀏覽器支援

- Chrome (最新版)
- Firefox (最新版)
- Safari (最新版)
- Edge (最新版)

## 📄 授權

ISC License

## 👥 作者

Your Name

## 🙏 致謝

感謝所有貢獻者和開源社群的支持。

---

**注意**: 目前後端使用模擬資料，實際部署時需要整合真實的資料庫和台股資料來源。
